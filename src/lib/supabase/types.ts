/**
 * Database types for the Supabase client.
 *
 * Placeholder until generated from the live schema. Queries are currently
 * untyped (`any`). Once your project is linked, generate real types with:
 *
 *   npx supabase gen types typescript --project-id oaqniknhecylacloexzb \
 *     > src/lib/supabase/types.ts
 *
 * That command overwrites this file with fully-typed tables matching
 * supabase/migrations/0001_init.sql, and every query becomes type-safe.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
