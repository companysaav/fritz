# Security: never run package lifecycle scripts (postinstall, etc.) on install.
# Blocks the most common supply-chain attack vector. Vet & run any genuinely
# needed build step manually.
ignore-scripts=true
